import Model from 'flarum/common/Model';

export default class AuditLog extends Model {
  // Define attributes if needed, but just registering the model is enough for Flarum to resolve the subject
  // You can add getters for attributes if you need to access them in the frontend
  // e.g., content = Model.attribute('content');
}
